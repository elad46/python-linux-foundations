# Linux Automation – Backup Script

This script creates a compressed backup of the user's home directory.
The backup file is named with the current date and stored in a backups directory.

## ScripthF
- Uses tar and gzip for compression
- Automatically creates backup directory if missing
- Demonstrates basic Linux automation with Bash
